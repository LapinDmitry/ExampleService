1. Для связи с БД нужно передать конфигурационную информацию в переменные окружения (см. handlers.go 24 строка)

2. До этого я не имел "боевого" опыта с http, генерированием файлов grpc и PostgresQL.
Поэтому, скорее всего, решение, возможно, кривое. Если бы были хорошие примеры, то по их подобию, я бы сделал лучше.

3. Я немного поменял протофайл, потому что в запросе ListUser() не хвататало аргументов page и limit, а в теле их передать нельзя, т.к. get запрос.

4. Не совсем ясна логика по редактированию Юзеров вместе с предметами.
Т.к. там можно передать и непривязанные к этому юзеру предметы.
И надо ли удалять предметы, которые были привязаны к юзеру раньше, но не были получены при команде update.

Я сделал самый простой вариант, я просто делаю update переданных с юзером предметов.

5. Увы с разобраться с миграцией структуры БД не хватило времени, я занимался grpc и самой базой.

6. У меня почему-то в транзакциях не работают запросы, которые идут после первого. Я оставил архитектуру под транзакции, надо только разобраться как их запустить.

7. Мог бы сделать нормальное логирование, конфигурационный файлик, прерывание долгих процессов через контекст, но это дополнительное время, я бы тогда совсем не успел.
